local M = {}

local events_ok, event = pcall(require, 'prelude').events
local event_t = type(event)

if not events_ok then
  if type(event) and type(event) ~= 'function' then

  end
end


return Msssssdw
r
