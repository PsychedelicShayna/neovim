return {
  "HiPhish/rainbow-delimiters.nvim",
  lazy = true,
  config = function()
    -- local rd_ok, rd = pcall(require, "rainbow-delimiters.setup")
    --
    -- if not rd_ok then
    --   PrintDbg("Failed to load rainbow-delimiters", LL_ERROR, { rd_ok, rd })
    --   return
    -- end
    --
    -- rd.setup()
  end
}
